# XFCE desktop image. The mid tier provides a usable desktop without a large
# application bundle.
source "${SRC}/userpatches/config-linknlink-common.conf"

BUILD_DESKTOP=yes
BUILD_MINIMAL=no
DESKTOP_ENVIRONMENT=xfce
DESKTOP_TIER=mid
LINKNLINK_IMAGE_FLAVOR=desktop
